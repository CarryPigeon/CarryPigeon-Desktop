/**
 * @fileoverview currentServer.ts 文件职责说明。
 */
import { currentServerSocket, getServerSocket, setServerSocket } from "./currentServerState";

export { currentServerSocket, getServerSocket, setServerSocket };
