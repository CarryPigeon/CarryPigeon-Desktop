/**
 * @fileoverview plugins｜presentation/store 公共入口（internal）。
 * @description
 * 该入口仅供 plugins feature 内部使用；跨 feature 请走 `@/features/plugins/api`。
 */

export { startPluginsRuntime } from "../../application/startPluginsRuntime";
